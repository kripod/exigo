import { IconButton, IconButtonProps } from '@chakra-ui/core';
import React from 'react';

export default function CarouselNavigationIconButton(props: IconButtonProps) {
  return <IconButton variant="ghost" size="lg" {...props} />;
}
