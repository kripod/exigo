{
  "private": true,
  "name": "exigo-server",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc --outDir ./dist/",
    "clean": "del-cli \"**/dev.db\" ./dist/ ./prisma/migrations/dev/ ./src/functions/graphql/nexus.generated.d.ts",
    "db:init": "prisma2 lift up --create-db",
    "db:seed": "ts-node --transpile-only ./prisma/seed.ts",
    "develop": "run-p develop:*",
    "develop:functions": "yarn build --watch",
    "develop:nexus": "onchange --verbose \"./src/functions/graphql/schema/**\" -- yarn generate:nexus",
    "develop:prisma": "prisma2 dev",
    "generate": "run-s generate:prisma generate:nexus",
    "generate:nexus": "cross-env NEXUS_SHOULD_GENERATE_ARTIFACTS=true ts-node --transpile-only ./src/functions/graphql/schema",
    "generate:prisma": "prisma2 generate",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@netlify/zip-it-and-ship-it": "^0.4.0-8",
    "@prisma/photon": "^2.0.0-preview017.2",
    "@types/bcryptjs": "^2.4.2",
    "@types/node-fetch": "^2.5.4",
    "apollo-server-lambda": "^2.9.12",
    "bcryptjs": "^2.4.3",
    "cross-env": "^6.0.3",
    "graphql": "^14.5.8",
    "nexus": "^0.12.0-rc.5",
    "nexus-prisma": "^0.6.1",
    "prisma2": "^2.0.0-preview017.2",
    "ts-node": "^8.5.3"
  },
  "devDependencies": {
    "del-cli": "^3.0.0",
    "onchange": "^6.1.0"
  }
}
